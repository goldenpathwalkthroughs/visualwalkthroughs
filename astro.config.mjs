import { defineConfig } from 'astro/config';

export default defineConfig({
  output: 'static',
  // site will be set to the Cloudflare Pages URL once connected
  // site: 'https://visualwalkthroughs.pages.dev',
});
