# dependencies
node_modules/
.pnp
.pnp.js

# build output
dist/
.astro/

# environment files — NEVER commit these
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local settings (machine-specific, not for the repo)
.claude/settings.local.json

# Cloudflare / Wrangler
.wrangler/
ops-dashboard.html

# Separate project living in this folder (the "tube ripper" app) — keep it out
# of the VisualWalkthroughs repo so a stray `git add -A` can't pull it in.
YouTube Downloader/
