# Game Queue

Add games here, one per line. The nightly pipeline picks the first unpublished entry.
Add `allowReplace: true` on the same line to permit overwriting an existing guide.

Format: `Game Title [platform] [optional notes]`

---

<!-- Add games below this line. The pipeline reads top-to-bottom. -->

